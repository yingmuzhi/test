/*
 * 1.学习程序设计理解计算机如何作用，程序是算法加ds
 * 2.了解变量类型，整型，浮点型，字符，字符串，指针变量。使用这些类型创建需要的数据结构
 * 3.了解if-else，while，for等操作，使用这些操作书写需要的逻辑
 * 4.静态本地变量static int i = 10;实质上是全局变量，下次进入这个方法后，该值i仍然是上次退出时候的值
 * 5.&i取地址符，取i值对应的地址
 *  *i解地址符，取地址i上对应的值*i
 */
